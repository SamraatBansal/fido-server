# For documentation on how to configure Diesel, see
# https://diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "src/schema.rs"
custom_type_derives = ["diesel::deserialize::FromSqlRow", "diesel::expression::AsExpression"]